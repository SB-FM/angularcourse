import { UnselectedPipe } from './unselected.pipe';

describe('UnselectedPipe', () => {
  it('create an instance', () => {
    const pipe = new UnselectedPipe();
    expect(pipe).toBeTruthy();
  });
});
